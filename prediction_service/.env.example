# Database (same credentials as backend)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=ecommerce_accrete

# Service
SERVICE_PORT=8000

# CORS - allow backend to call this service
ALLOWED_ORIGINS=http://localhost:3000
